# 蝦霸 - 蝦幣自動化採集工具 系統流程說明 (System Workflow)

## 1. 系統核心架構 (System Architecture)
本工具整合了 Android 三大核心服務，實現全自動化的直播間蝦幣採集：
*   **MediaProjection Service**: 負責即時螢幕擷取，將畫面傳送給辨識引擎。
*   **ML Kit Text Recognition**: 使用 Google 機器學習模型進行光學字元辨識 (OCR)。
*   **Accessibility Service**: 負責執行模擬手勢（點擊、滑動、返回），實現無人值守操作。
*   **三路並行辨識管道 (Triple-Path OCR Pipeline)**：同時使用原圖、金色增強圖、白色增強圖進行多維度辨識。

---

## 2. 影像增強濾鏡 (Image Enhancement Filters)
針對直播間複雜的背景干擾，系統開發了兩套專屬濾鏡，將辨識率提升至近乎 100%：

### 金色增強濾鏡 (Gold Filter)
*   **專攻目標**：金黃色的蝦幣數字（如 `$ 0.48`）。
*   **參數設定**：`2.0R + 2.0G - 4.0B - 100。
*   **原理**：強化紅/綠通道並重罰藍色（互補色），能有效將黃金數字從複雜背景或半透明藍底中完美提取，並將其餘部分壓為純黑。

### 白色增強濾鏡 (White Filter)
*   **專攻目標**：白色文字標題、倒數時間、領取與重試按鈕。
*   **參數設定**：`5.0R + 5.0G + 5.0B - 3000` (極端高門檻二值化)。
*   **原理**：設定極高的亮度門檻（約 200），只有接近純白的文字能存活，成功過濾掉所有有色雜訊與紅/藍色背景塊。

---

## 3. 核心偵測流程 (Core Detection Logic)
每一幀畫面的處理都透過三路 OCR 結果進行精準匹配：

### 第一階段：標題定位 (Header Search)
*   使用裁切圖尋找「直播間蝦幣」或「任務獎勵」關鍵字。
*   鎖定標題座標，作為後續數字位置判定的基準點。

### 第二階段：數值驗證 (Value Validation)
*   在標題下方搜尋符合門檻（例如 `0.4`）的數字。
*   檢查數字是否位於標題的正下方且水平對齊，避免誤觸畫面其他區域。
*   若數值符合目前時段設定的 `Goal` 門檻，進入下一階段。

### 第三階段：時間監控 (Timer Check)
*   搜尋標題下方的倒數計時器（格式如 `05:30`）。
*   若偵測到時間，狀態轉為 **WAITING_COIN (等待中)**。
*   系統進入智慧節能模式，將偵測頻率降低至 **12秒/次**。

### 第四階段：動作執行 (Action Handling)
*   **領取按鈕**：一旦出現「領取」字樣，立即計算精準座標並執行**隨機偏移點擊 (±3px)**。
*   **重試按鈕**：若出現「重試」或網路連線問題，自動觸發 `QuickRefresh` (滑走再滑回) 修復 UI。

### 第五階段：異常處理 (Stability Recovery)
*   **狀態豁免機制**：在等待期間若 OCR 偶發性沒認到內容，提供 **2 次豁免權**，避免立刻跳頁導致前功盡棄。
*   **卡死檢測**：若長時間卡在同一狀態或找不到幣，自動執行 `MoveNextPage` 尋找下一個目標。

---

## 3. 智慧搜尋策略 (Smart Search Strategy)
系統根據 `DefaultStrategies` 定義的不同時段自動切換採集行為，本質上是透過「期望值（Goal）」來篩選優質直播間：

1.  **時段適配**：每個時段擁有獨立的 `CoinStrategy` 配置。
2.  **初始高期望值搜尋**：搜尋開始時，期望值會設定為 `PeriodUpValue`（時段最高值），採集程序會一間一間直播間往下滑動，尋找大於等於該值的目標。
3.  **全域刷新（洗牌機制）**：
    *   若連續搜尋 `RefreshCount` 個房間仍未達標，系統會執行 `FullFreshPage`。
    *   目的在於重新觸發蝦皮的推薦演算法，打亂並刷新直播間順序，重新尋找高額蝦幣目標。
4.  **階梯式門檻下修**：
    *   每當觸發一次全域刷新，目前的期望值會自動減去 `CoinValueMinus`。
    *   這確保了在當前環境缺乏高額蝦幣時，系統能自動降低標準，增加採集成功率。
5.  **保底門檻（最低期望值）**：期望值下修的終點為 `PeriodDownValue`，系統不會搜尋低於此底線的直播間。
6.  **成功後的重置機制**：一旦成功領取蝦幣，期望值會立即恢復至 `PeriodUpValue` 並重新開始搜尋，以捕捉可能新出現的高價值直播間。
7.  **自動導航與省電 (optional)**：
    *   若意外跳出直播頁面，系統會嘗試透過導航手勢回到直播間。
    *   若不在任何排程時段內，會自動返回 App 主介面以節省系統資源與電量。
8.  **每日採集上限與自動停止 (Daily Limit Auto-Stop)**：
    *   **智慧統計**：系統會自動計算當天（自凌晨 00:00 起）的總領取次數與金額。
    *   **自動停止機制**：當今日採集次數達到 **100 次** 時，系統會自動將偵測功能切換為「暫停」狀態，並在懸浮窗顯示「今天已領滿」。
    *   **每日重置**：該停止狀態以日期為標籤（Day of Year），跨日後會自動解除限制重新計數。
    *   **手動覆蓋**：自動停止每天僅觸發一次。若使用者在觸發後仍想繼續，可手動點擊懸浮按鈕恢復運行，系統當天將不再干預。

---

## 4. UI 與 Debug 介面
*   **懸浮狀態燈**：
    *   `亮灰色`: WAITING_COIN (已鎖定目標，等待領取)。
    *   `深灰色`: 其他狀態 (尋找中、處理中、暫停)。
*   **實時響應式 Debug 訊息 (Reactive UI)**：
    *   採用 `mutableStateOf` 建立全局狀態，只要辨識到資訊，懸浮窗訊息會即時跳動（零延遲）。
    *   **Goal**: 目前門檻數值。
    *   **CoinVal**: 偵測到的座標與數值（持續保留直到換頁）。
    *   **Time**: 目前偵測到的倒數時間 (Time: XX:XX)。
*   **影像偵錯模式 (Image Debug)**：
    *   獨立開關。開啟後，系統會將處理後的 `GoldFilter` 與 `WhiteFilter` JPG 圖片直接存入手機相簿的 `ShopC_Debug` 資料夾。
*   **動態滑動調校 (Swipe Tuning)**：
    *   **即時調整**：提供「距離係數」與「滑動時間」的動態修改介面。
    *   **背景測試**：支援在背景模式下進行「上滑/下滑」循環測試，方便使用者切換至蝦皮直接觀察參數調整後的手勢穩定性。
    *   **參數持久化**：所有調校後的參數會自動儲存，下次啟動 App 時無需重新設定。

---

## 5. 安全與防偵測 (Anti-Detection)
*   **隨機化座標**：點擊位置不再是死板的像素點，而是帶有隨機偏移的圓形區域。
*   **貝茲曲線滑動**：換頁手勢採用模擬真人的曲線軌跡，而非直線，避開系統對自動化工具的特徵檢測。

---

## 6. 優化：回溯機制 (Backtracking Navigation)
為了避免頻繁的全域刷新（Full Refresh）導致時間浪費，引入「記憶回溯」邏輯：

### 核心邏輯
當搜尋次數達到 `RefreshCount` 仍未發現符合當前 `Goal` 的蝦幣時，系統在調降門檻（下修期望值）後，會優先檢查**剛才滑過的房間歷史紀錄**，而不是立即重新洗牌。

### 運作流程圖
```text
[ 開始搜尋 ]
     |
     v
[ 順向滑動 (Next) ] ----> [ 紀錄房間資訊 (Index, Value, Time) ]
     |
     v
[ 達到 RefreshCount 且未達標 ]
     |
     v
[ 計算新門檻 (Goal - Minus) ]
     |
     v
[ 檢查歷史紀錄 ] <---------------------------+
     |                                      |
     +-- ( 有符合新門檻的房 ) --> [ 執行回溯滑動 (Previous) ]
     |                                      |
     +-- ( 無符合紀錄 ) --------> [ 執行 Full Refresh (洗牌) ]
                                            |
                                            v
                                     [ 清空歷史紀錄 ]
```

### 優點
1.  **零載入時間**：回滑 3-5 頁僅需幾秒，且不需要等待網路重新加載直播流。
2.  **確定性收益**：比起重新洗牌的不確定性，回溯已知的優質直播間能保證獲取蝦幣。
3.  **時效保護**：歷史紀錄設有 5 分鐘 TTL (Time-To-Live)，確保回溯的目標房蝦幣尚未過期。
4.  **穩定性控制**：回溯過程中，每頁滑動間隔設為 **5 秒**，確保直播間畫面完全載入並穩定後才進行辨識或下一步動作，避免 OCR 在畫面震動時發生誤判。
